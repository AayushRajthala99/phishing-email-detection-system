# MongoDB Configuration

MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=securepassword123

# Note: The MongoDB container is NOT exposed to the public internet.
# It only accepts connections from the backend service within the Docker network.